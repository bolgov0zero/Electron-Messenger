Починен счётчик непрочитанных на иконке приложения — раньше он обновлялся только на Mac. На Linux теперь тоже работает (в окружениях с поддержкой Unity/DBus — это зависит от самого окружения). На Windows добавлен красный кружок с числом поверх иконки в панели задач — раньше там счётчика не было вовсе.
