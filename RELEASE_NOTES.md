В инструкцию по вебхукам в админ-панели добавлен раздел про n8n: как правильно настроить ноду HTTP Request (тело запроса вместо query-параметров, режим Using Fields Below) и почему переносы строк нужно заменять на <br>.
